---
title: Workshop Overview
description: An introduction to the ltk-manager Creator Workshop, the integrated environment for building and authoring League of Legends mods.
---

import { FileTree, Tabs, TabItem } from '@astrojs/starlight/components';
import ProjectScaffold from '../../../components/ProjectScaffold.svelte';

The Creator Workshop is where you build, edit, and package mods. It provides a project-based workflow for mod development.

## Getting Started

### Setting Up the Workshop

1. Go to [Settings](/tools/ltk-manager/configuration/)
2. Set the **Workshop Path** - this is the directory where your projects live
3. Navigate to the **Workshop** tab

### Workshop Path Structure

{/* prettier-ignore */}
<FileTree>
- workshop-path/
  - my-skin-mod/
    - mod.config.json
    - thumbnail.webp
    - content/
      - base/
        - Ahri.wad.client/
          - … (mod files)
  - another-mod/
    - mod.config.json
    - content/
      - base/
        - Ahri.wad.client/
          - … (mod files)
      - high-res/
        - Ahri.wad.client/
          - … (mod files)
</FileTree>

Each subdirectory containing a `mod.config.json` (or `mod.config.toml`) is recognized as a project.

## Creating a Project

Click **New Project** and fill in:

- **Name** - Slug name used for the directory (lowercase, hyphens, no spaces)
- **Display Name** - Human-readable name shown in the library
- **Description** - What the mod does
- **Authors** - Creator names and optional roles

## Project Configuration

The project config file stores all project metadata. LTK Manager supports both `mod.config.json` and `mod.config.toml`; the full field reference is on [Mod Projects](/making-mods/mod-projects/#the-manifest):

<Tabs>
  <TabItem label="JSON">
    ```json
    {
      "name": "my-skin-mod",
      "display_name": "My Skin Mod",
      "version": "1.0.0",
      "description": "A custom skin for Ahri",
      "authors": [
        { "name": "ModderName", "role": "Creator" }
      ],
      "tags": ["skin"],
      "champions": ["Ahri"],
      "maps": [],
      "layers": [
        {
          "name": "base",
          "priority": 0,
          "description": "Base skin files"
        }
      ]
    }
    ```
  </TabItem>
  <TabItem label="TOML">
    ```toml
    name = "my-skin-mod"
    display_name = "My Skin Mod"
    version = "1.0.0"
    description = "A custom skin for Ahri"
    tags = ["skin"]
    champions = ["Ahri"]
    maps = []

    [[authors]]
    name = "ModderName"
    role = "Creator"

    [[layers]]
    name = "base"
    priority = 0
    description = "Base skin files"
    ```

  </TabItem>
</Tabs>

### Try It

Use the interactive builder below to see how your project configuration and file tree structure look together:

<ProjectScaffold client:visible />

## Editing Projects

Open a project to access the editor, where you can:

- Edit display name, version, and description
- Manage authors (add, remove, set roles)
- Set tags, champions, and maps
- Configure layers
- Edit [string overrides](/making-mods/string-overrides/) to change in-game text
- Set a thumbnail image

## Layers

Layers organize your mod's content into separate groups. Common uses:

- **base** - Core mod files (required)
- **high-res** - Optional high-resolution textures
- **alternate** - Alternative versions or color variants
- **particles** - Optional particle effect modifications

Each layer's directory contains `.wad.client` folders that assign raw files to their target WAD. The manager builds the WAD archives at overlay time. Each layer has a priority that determines load order when multiple layers are active. See [Layers](/making-mods/layers/) for details.

## Importing Projects

You can create projects from existing sources:

- **[From .modpkg](/making-mods/project/#from-modpkg)** - Extract an existing mod package into a project
- **[From .fantome](/making-mods/project/#from-fantome)** - Import a fantome archive as an editable project
- **[From fantome WAD/RAW folders](/making-mods/project/#from-fantome-wadraw-folders)** - Migrate raw files from fantome folder structures
- **[From Git](/making-mods/project/#from-git)** - Clone a Git repository as a project

## Packing and Exporting

When your mod is ready for distribution:

1. Click **Pack** on your project
2. Choose the output format:
   - **fantome** - what to pick when publishing to a mod site, none of which support modpkg yet
   - **modpkg** - full metadata, multi-layer, thumbnail support; use it when distributing outside a platform
3. Choose the output location
4. The packed file is ready to share

See [Choosing a format](/making-mods/packaging/#choosing-a-format) for which to
pick, and why the more capable format is not always the right upload.

## Validation

Before packing, run **Validate** to check for issues. Validation checks for:

- Required configuration fields
- Valid layer structure
- File path conflicts between layers
- Configuration syntax errors

Warnings suggest improvements; errors must be fixed before packing.

## Related Pages

- [Layers](/making-mods/layers/) - Layer system details
- [String Overrides](/making-mods/string-overrides/) - Editing in-game text
- [Importing Projects](/making-mods/project/) - Import from various sources
- [Mod Package Format](/making-mods/packaging/) - Technical format specification
